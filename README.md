# lamp-stack-terraform

## Guide

Using Terraform 0.12 export your AWS KEYs and:

cd environments/aws/dev
terraform init
terraform apply -auto-approve

You can see the link of wordpress site after running.